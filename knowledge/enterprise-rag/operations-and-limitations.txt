AI Orchestra uses deterministic lexical retrieval over this committed synthetic corpus. It does not use embeddings, vector storage, uploads, managed file search, production connectors, or persistent runs. Browser edits, questions, answers, evidence bindings, and exports remain in memory and reset on reload or a new browser context.

The relational database node is simulated. The runtime never opens its configured connection and never queries a database. The advisory database edge is ignored during execution. Tools, handoffs, persistence, remote tracing, uploads, and live connectors remain absent or disabled.

Portable judge execution requires explicit judge_fixture mode and makes one in-process deterministic adapter invocation with zero provider requests. Usage and provider timing are synthetic fixture evidence; external API cost is zero, while local compute is not measured as free. The optional live Ollama path requires explicit ollama_local mode, loopback qwen3:4b availability, and separate host resources. There is no provider fallback.

Runs retain bounded request size, input length, timeout, tokens, process-local concurrency, and six-attempt fixed-window rate controls. The rate limiter resets with the app process and is not distributed quota enforcement. The judge deployment is a hackathon portability path, not production operations, multi-tenant isolation, certification, penetration testing, or zero risk.
